/*
Write a program to find the factorial of a number using recursive functions.

Function Specification:

int factorial(int)
 

Input Format:

Input consists of a single integer.

Output Format:

Output consists of a integer which corresponds to the factorial of a number.
 

Sample Input:

5

Sample Output:

120


Function Definitions:

int  factorial (int) 
*/







#include<stdio.h>
int factorial(int n)
{
    if(n==0)
        return 1;
    else
        return n*factorial(n-1);
    
}
int main()
{
	int n;
	scanf("%d",&n);
	printf("%d",factorial(n));
	
	
	return 0;
}
